<form method="post" action="{{ path('app_todo_list_delete', {'id': todoList.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ todoList.id) }}">
    <button class="btn">Delete</button>
</form>
