{% extends 'base.html.twig' %}

{% block title %}TodoList{% endblock %}

{% block body %}
    <h1>TodoList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ todoList.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ todoList.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_todo_list_index') }}">back to list</a>

    <a href="{{ path('app_todo_list_edit', {'id': todoList.id}) }}">edit</a>
{% endblock %}
